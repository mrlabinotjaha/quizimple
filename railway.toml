[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildArgs = ["VITE_GOOGLE_CLIENT_ID"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
